package userProfile

import "github.com/frclba/slick/app/models"

templ Index(user models.User) {
    <h1>User profile page {user.Email}</h1>
    <h1>{user.FirstName}</h1>
    <h1>{user.LastName}</h1>
}