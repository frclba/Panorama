package profile

import "github.com/frclba/slick/app/model"

type User struct {
	FirstName string
	LastName  string
	Email     string
}


templ Index(user model.User) {
    <h1>User profile page {user.Email}</h1>
    <h1>{user.FirstName}</h1>
    <h1>{user.LastName}</h1>
}